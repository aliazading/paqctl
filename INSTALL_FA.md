# راهنمای نصب paqctl در سرورهای با دسترسی محدود (ایران)

اگر سرور شما به دلیل محدودیت‌های اینترنت بین‌الملل نمی‌تواند فایل‌های مورد نیاز را از گیت‌هاب دانلود کند، از یکی از دو روش زیر استفاده کنید.

---

## روش اول: قرار دادن فایل فشرده به صورت دستی (پیشنهادی)

در این روش، شما فایل را خودتان دانلود کرده و در مسیر مشخص قرار می‌دهید تا اسکریپت از آن استفاده کند.

### ۱. ایجاد پوشه مورد نیاز
ابتدا دستور زیر را بزنید تا پوشه نصب ایجاد شود:
```bash
sudo mkdir -p /opt/paqctl/bin/
```

### ۲. دانلود فایل فشرده
فایل فشرده مناسب با معماری پردازنده خود را از [صفحه ریلیزهای paqet](https://github.com/hanselime/paqet/releases) دانلود کنید:
*   برای اکثر سرورها (64 بیتی): `paqet-linux-amd64-v1.0.0-alpha.19.tar.gz`
*   برای سرورهای ARM: `paqet-linux-arm64-v1.0.0-alpha.19.tar.gz`

### ۳. انتقال فایل به سرور
فایل دانلود شده را **بدون تغییر نام و بدون استخراج** به مسیر `/opt/paqctl/bin/` در سرور منتقل کنید.

### ۴. اجرای اسکریپت نصب
حالا اسکریپت را اجرا کنید. اسکریپت به طور خودکار فایل را در آن مسیر پیدا کرده و از آن استفاده می‌کند:
```bash
curl -fsSL https://raw.githubusercontent.com/aliazading/paqctl/main/paqctl.sh | sudo bash
```
*(نکته: از لینک بالا که نسخه اصلاح شده است استفاده کنید)*

---

## روش دوم: استفاده از لینک کمکی (Mirror)

اگر یک هاست داخلی یا لینک مستقیم دارید که سرور به آن دسترسی دارد، می‌توانید از قابلیت Mirror استفاده کنید.

کافیست قبل از اجرای اسکریپت، متغیر `PAQET_MIRROR` را با لینک فایل تنظیم کنید:

```bash
export PAQET_MIRROR="https://your-mirror-link.com/paqet-linux-amd64-v1.0.0-alpha.19.tar.gz"
curl -fsSL https://raw.githubusercontent.com/aliazading/paqctl/main/paqctl.sh | sudo bash
```

در این حالت اسکریپت به جای گیت‌هاب، فایل را از لینک شما دانلود می‌کند.

---

## نکات مهم
*   **معماری پردازنده:** با دستور `uname -m` می‌توانید معماری را چک کنید (x86_64 یعنی amd64).
*   **فایل‌های تنظیمات:** اسکریپت به صورت خودکار فایل‌های `yaml.example` را از داخل فایل فشرده استخراج کرده و در `/opt/paqctl/` قرار می‌دهد، پس نگران آن‌ها نباشید.
*   **منوی مدیریت:** بعد از نصب همیشه می‌توانید با دستور `sudo paqctl menu` تنظیمات را تغییر دهید.
