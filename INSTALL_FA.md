# راهنمای نصب paqctl در سرورهای با دسترسی محدود به اینترنت (ایران)

اگر سرور شما (مثلاً در ایران) به دلیل محدودیت‌های اینترنتی نمی‌تواند فایل‌های مورد نیاز را از گیت‌هاب دانلود کند، می‌توانید با استفاده از این راهنما، نصب را به صورت دستی انجام دهید.

## مراحل نصب دستی

### ۱. ایجاد پوشه مورد نیاز
ابتدا پوشه مربوط به باینری‌های برنامه را در مسیر استاندارد ایجاد کنید:

```bash
sudo mkdir -p /opt/paqctl/bin/
```

### ۲. دانلود فایل فشرده paqet
فایل فشرده (`.tar.gz`) متناسب با معماری پردازنده سرور خود را از صفحه [Releases پروژه paqet](https://github.com/hanselime/paqet/releases) دانلود کنید.

*   برای اکثر سرورها (64 بیتی): `paqet-linux-amd64-v1.0.0-alpha.19.tar.gz`
*   برای سرورهای ARM: `paqet-linux-arm64-v1.0.0-alpha.19.tar.gz`

### ۳. قرار دادن فایل در مسیر صحیح
فایل دانلود شده را (بدون استخراج) به مسیر `/opt/paqctl/bin/` منتقل کنید:

```bash
# مثال برای سیستم های 64 بیتی
sudo cp paqet-linux-amd64-v1.0.0-alpha.19.tar.gz /opt/paqctl/bin/
```

**نکته:** اسکریپت نصب به طور خودکار این فایل را شناسایی کرده، آن را استخراج می‌کند و فایل‌های تنظیمات لازم (yaml) را در محل مناسب قرار می‌دهد.

### ۴. اجرای اسکریپت نصب
حالا می‌توانید اسکریپت `paqctl.sh` را اجرا کنید. اسکریپت به صورت خودکار متوجه وجود فایل فشرده می‌شود و مرحله دانلود را رد می‌کند:

```bash
sudo bash paqctl.sh
```

## تشخیص معماری پردازنده
اگر مطمئن نیستید که کدام نسخه را باید دانلود کنید، دستور زیر را در سرور اجرا کنید:

```bash
uname -m
```
*   اگر خروجی `x86_64` بود: نسخه **amd64** را دانلود کنید.
*   اگر خروجی `aarch64` بود: نسخه **arm64** را دانلود کنید.

## استفاده از منوی مدیریت
پس از نصب، می‌توانید از دستور زیر برای مدیریت برنامه استفاده کنید:

```bash
sudo paqctl menu
```
